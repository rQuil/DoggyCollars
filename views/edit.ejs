<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit <%= item.title %> - DoggyCollars</title>
  <link rel="icon" type="image/x-icon" href="/images/tablogo.webp">
  <link rel="stylesheet" href="/CSS/new.css">
  <link rel="stylesheet" href="/CSS/headFoot.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
  <%- include('partials/header') %>
  <main>
    <div class="main-content">
      <div class="form-text">
        <h2>Edit Item</h2>
        <p>Update the details of your item below.</p>
      </div>
      <div class="form-container">
        <form action="/items/<%= item.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
          <label for="title">Title:</label>
          <input type="text" id="title" name="title" value="<%= item.title %>" required>

          <label for="condition">Condition:</label>
          <select id="condition" name="condition" required>
            <option value="New" <%= item.condition === 'New' ? 'selected' : '' %>>New</option>
            <option value="Like new" <%= item.condition === 'Like new' ? 'selected' : '' %>>Like new</option>
            <option value="Very Good" <%= item.condition === 'Very Good' ? 'selected' : '' %>>Very Good</option>
            <option value="Good" <%= item.condition === 'Good' ? 'selected' : '' %>>Good</option>
            <option value="Other" <%= item.condition === 'Other' ? 'selected' : '' %>>Other</option>
          </select>

          <label for="price">Price:</label>
          <input type="number" id="price" name="price" step="0.01" value="<%= item.price %>" required>

          <label for="details">Details:</label>
          <textarea id="details" name="details" rows="4" required><%= item.details %></textarea>

          <!-- update image with file -->
          <label for="image">Upload New Image:</label>
          <input type="file" id="image" name="image">

          <button type="submit" class="btn">Update</button>
        </form>
      </div>
    </div>
  </main>
  <%- include('partials/footer') %>
</body>
</html>